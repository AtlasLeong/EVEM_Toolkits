`market_catalog.json` is a read-only item ID/name catalog exported by the user's
experimental EVE market collector on 2026-09-24. It contains market metadata,
not quotes, credentials, sessions, or packet captures. Prices still require a
live, authorized collection session. Run `manage.py market_seed_catalog` after
the Market migration to insert catalog entries disabled by default.
